#ifndef UTILS_HPP
#define UTILS_HPP

#include <iostream>
#include <vector>
#include <string>
#include <sstream>
#include <fstream>
#include <iomanip>

std::vector<std::string> splitStr(const std::string& s, char delimiter);

#endif  // UTILS_HPP
